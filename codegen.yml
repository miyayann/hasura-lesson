overwrite: true
schema: "https://strong-firefly-45.hasura.app/v1/graphql"
documents: "queries/**/*.ts"
generates:
  types/generates/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
